{
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "test": "node --test tests/Frontend/*.test.mjs",
        "watch": "vite build --watch"
    },
    "devDependencies": {
        "laravel-vite-plugin": "^2.0.0",
        "vite": "^7.0.0"
    }
}
